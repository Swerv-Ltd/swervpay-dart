/// Card Issuer
enum CardIssuer {
  /// Mastercard
  mastercard,

  /// Visa
  visa,
}

/// Card Type
enum CardType {
  /// lite card
  lite,

  /// Default card
  defaultCard,

  /// Coorporate card
  corporate,
}

/// Currency

enum Currency {
  /// USD
  usd,

  /// NGN
  ngn,
}

/// Collection Type
enum CollectionType {
  /// one time
  // ignore: constant_identifier_names
  one_time,

  /// Default
  defaultType,
}
